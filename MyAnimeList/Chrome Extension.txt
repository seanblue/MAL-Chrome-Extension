** Features **

['@' = not done]
['#' = done]

#1. Show popover with additional information.
	# Title
	@ English Title (if available)
	# Average Score ('members_score')
	# Rank (rank)
	# Popularity (popularity_rank)
	# Number of Favorites ('favorited_count')
	# Genres (genres[])
	# Date Aired ('start_date' and 'end_date'; myDate.toLocaleDateString(); make sure to check null for '?' such as animeid=25777)
#2. Filtering
	# Genre (genres[])
	# Type (type) ['TV', 'Movie', etc.]
	# Rating (classification) ['PG-13', etc. but with the description afterward]
	# Tag (would require page parsing)
	# Title (quicker than scrolling; check alternate titles)
	# Synopsis
	# Status
@3. Sorting
	@ Title
	# Average Score
	# Rank
	# Popularity
	# Number of Favorites
	# Rating
	@ Start Date and End Date (how to compare year only or unknown dates?)
	# Number of episodes ('episodes') [currently only sorts on total episodes watched, so it's useless in "Plan to Watch"]
	# Automatically sort alphabetically as a secondary sort.
	@ Reverse sort (check box).
@4. Move Filter/Sorting to area below main black bar to allow for more room and customization of styles.